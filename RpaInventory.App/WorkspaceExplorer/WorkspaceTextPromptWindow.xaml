<Window x:Class="RpaInventory.App.WorkspaceExplorer.WorkspaceTextPromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Entrada"
        Width="520"
        Height="200"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FF1B1B1B"
        Foreground="#FFF6F6F6">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageText"
                   Grid.Row="0"
                   TextWrapping="Wrap"
                   FontSize="13"
                   Margin="0,0,0,10" />

        <TextBox x:Name="ValueTextBox"
                 Grid.Row="1"
                 Height="28"
                 Background="#FF2A2A2A"
                 BorderBrush="#FF4A4A4A"
                 BorderThickness="1"
                 Foreground="#FFF6F6F6"
                 Padding="6,4"
                 VerticalContentAlignment="Center" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,14,0,0">
            <Button Width="90"
                    Height="30"
                    Margin="0,0,10,0"
                    Background="#FF7A7A7A"
                    BorderBrush="#FF2D2D2D"
                    BorderThickness="1"
                    Click="Ok_Click">
                <TextBlock Text="OK" FontWeight="SemiBold" />
            </Button>
            <Button Width="90"
                    Height="30"
                    Background="#FF3A3A3A"
                    BorderBrush="#FF2D2D2D"
                    BorderThickness="1"
                    Click="Cancel_Click">
                <TextBlock Text="Cancelar" />
            </Button>
        </StackPanel>
    </Grid>
</Window>

